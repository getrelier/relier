name: Relier Docs CI/CD

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "mkdocs.yml"
      - ".github/workflows/docs.yml"
  pull_request:
    branches: [main]
    paths:
      - "docs/**"
      - "mkdocs.yml"

jobs:
  validate-and-deploy:
    name: Build and Verify Documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Strict Build Validation Test
        run: uv run --with mkdocs-material --with pymdown-extensions mkdocs build --strict

      - name: Deploy to GitHub Pages (Main Branch Only)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: uv run --with mkdocs-material --with pymdown-extensions mkdocs gh-deploy --force
